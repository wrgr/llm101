const glossary = {
  token: {
    term: "Token",
    short: "A subword unit of text that LLMs process. Words are split into tokens by a tokenizer before being fed to the model.",
    long: "Tokens are the atomic units that language models operate on. A token is typically a word, part of a word, or a punctuation character. For example, 'unbelievable' might be split into ['un', 'believ', 'able']. On average, one token corresponds to roughly 3-4 characters of English text. Models have a fixed vocabulary of tokens they recognize.",
    seeAlso: ["tokenizer", "vocabulary", "context-window"],
  },
  tokenizer: {
    term: "Tokenizer",
    short: "A component that maps raw text to integer IDs from the model's vocabulary, and converts IDs back to text.",
    long: "Tokenizers use algorithms like Byte-Pair Encoding (BPE) or WordPiece to segment text into subword units. Each unique token in the vocabulary gets a unique integer ID. The tokenizer first splits text into tokens, then looks up each token's ID. This vocabulary is fixed at training time and shared between the model and any application using it.",
    seeAlso: ["token", "vocabulary", "embedding"],
  },
  embedding: {
    term: "Embedding",
    short: "A dense vector representation of a token or sequence that captures semantic meaning in a high-dimensional space.",
    long: "Embeddings are learned mappings from discrete tokens (integers) to continuous vectors. Tokens with similar meanings tend to have vectors that are geometrically close. The embedding layer is typically the first layer in a transformer, converting token IDs into vectors. Embeddings can also refer to the output of an encoder model — a fixed-size vector representing an entire input sequence.",
    seeAlso: ["token", "tokenizer", "transformer", "position-encoding"],
  },
  attention: {
    term: "Attention",
    short: "A mechanism that allows a model to weigh the relevance of different positions in a sequence when computing a representation.",
    long: "Attention computes a weighted sum of value vectors, where the weights (attention scores) are determined by the similarity between a query vector and a set of key vectors. This allows the model to selectively focus on relevant parts of the input when producing each output token. The attention formula is: Attention(Q,K,V) = softmax(QK^T / sqrt(d_k)) * V.",
    seeAlso: ["self-attention", "multi-head-attention", "head", "transformer"],
  },
  "self-attention": {
    term: "Self-attention",
    short: "A form of attention where queries, keys, and values all come from the same sequence, letting each token attend to every other token.",
    long: "In self-attention, a single input sequence provides all three components: queries, keys, and values. Each token can attend to every other token in the sequence, capturing long-range dependencies. This is the core building block of transformer models. In decoder models, causal (masked) self-attention prevents tokens from attending to future positions during training.",
    seeAlso: ["attention", "multi-head-attention", "transformer"],
  },
  transformer: {
    term: "Transformer",
    short: "The dominant neural network architecture for LLMs, built from stacked layers of self-attention and feed-forward networks.",
    long: "Introduced in 'Attention Is All You Need' (Vaswani et al., 2017), the transformer replaced recurrent architectures with parallel self-attention layers. A transformer consists of an embedding layer, N repeated blocks (each containing multi-head self-attention, a feed-forward network, layer normalization, and residual connections), and a final output projection. Transformers scale well with data and compute, enabling modern LLMs.",
    seeAlso: ["self-attention", "multi-head-attention", "feed-forward", "layer-norm", "residual", "position-encoding"],
  },
  llm: {
    term: "Large Language Model",
    short: "An autoregressive language model trained at massive scale on text data, capable of generating coherent and useful language.",
    long: "LLMs are transformer-based models with billions to trillions of parameters, pretrained on vast corpora of text via next-token prediction. Scale in data, compute, and model size leads to emergent capabilities not seen in smaller models. Examples include GPT-4, Claude, Llama, and Gemini. LLMs are typically fine-tuned and aligned after pretraining for safe and helpful behavior.",
    seeAlso: ["transformer", "pretraining", "next-token-prediction", "autoregressive", "emergent", "alignment"],
  },
  "next-token-prediction": {
    term: "Next-token prediction",
    short: "The core training objective of autoregressive LLMs: predict the next token given all previous tokens in the sequence.",
    long: "During pretraining, the model is given a sequence of tokens and must predict what comes next at each position. The loss is computed as cross-entropy between the predicted probability distribution and the true next token. Minimizing this objective over trillions of tokens forces the model to learn grammar, facts, reasoning, and style. At inference time, this same process is repeated to generate new text.",
    seeAlso: ["autoregressive", "cross-entropy", "loss", "pretraining", "logit", "softmax"],
  },
  softmax: {
    term: "Softmax",
    short: "A function that converts a vector of raw scores (logits) into a valid probability distribution that sums to 1.",
    long: "Softmax(x_i) = exp(x_i) / sum(exp(x_j)). By exponentiating each logit and dividing by the total, softmax produces positive values that sum to one. It amplifies differences between scores: the highest logit gets a disproportionately large probability. In LLMs, softmax is applied to the final layer's logits to produce the next-token probability distribution. Temperature scaling modifies the logits before softmax.",
    seeAlso: ["logit", "temperature", "sampling", "next-token-prediction"],
  },
  logit: {
    term: "Logit",
    short: "The raw, unnormalized score output by the model's final linear layer for each token in the vocabulary before softmax.",
    long: "For each position in the sequence, the transformer's final layer projects hidden states into a vector of size |vocabulary|. Each entry is a logit — an unconstrained real number indicating how likely that token is next. Higher logits become higher probabilities after softmax. Logits can be manipulated before sampling: temperature scaling, top-k filtering, and repetition penalties all operate on logits.",
    seeAlso: ["softmax", "temperature", "sampling", "vocabulary"],
  },
  temperature: {
    term: "Temperature",
    short: "A scalar that controls the sharpness of the token probability distribution, tuning output randomness.",
    long: "Temperature T is applied by dividing all logits by T before the softmax: softmax(logits / T). Temperature < 1 makes the distribution sharper (more deterministic, picks likely tokens). Temperature > 1 flattens the distribution (more random, explores unlikely tokens). Temperature = 1 is the default (unchanged). Temperature = 0 approaches greedy decoding. Choosing the right temperature trades off coherence vs. creativity.",
    seeAlso: ["logit", "softmax", "sampling", "greedy-decoding"],
  },
  sampling: {
    term: "Sampling",
    short: "Drawing the next token from the model's probability distribution rather than always picking the most likely token.",
    long: "Sampling introduces stochasticity into generation, producing varied outputs for the same prompt. Common variants include top-k sampling (restrict to k most likely tokens), nucleus/top-p sampling (restrict to tokens whose cumulative probability exceeds p), and temperature scaling. Sampling is preferred over greedy decoding for creative tasks but can produce incoherent outputs at high temperatures.",
    seeAlso: ["temperature", "softmax", "logit", "greedy-decoding", "beam-search"],
  },
  hallucination: {
    term: "Hallucination",
    short: "When a model generates text that is fluent and plausible but factually incorrect or entirely fabricated.",
    long: "Hallucinations arise because LLMs are trained to produce likely text, not necessarily true text. The model may confidently state false facts, invent citations, or fabricate details. Hallucinations are a fundamental challenge because the model has no direct access to ground truth during generation. Mitigations include retrieval-augmented generation, grounding, better training data, and explicit uncertainty prompting.",
    seeAlso: ["grounding", "retrieval", "next-token-prediction"],
  },
  grounding: {
    term: "Grounding",
    short: "Tethering model outputs to verified external sources or facts to reduce hallucination.",
    long: "Grounding connects the model's generation to trusted information sources — documents, databases, knowledge graphs, or tool outputs. A grounded model should only assert things it can attribute to provided sources. Retrieval-augmented generation is a common grounding technique. Grounding improves factual accuracy but requires good retrieval and the model's ability to faithfully report retrieved content.",
    seeAlso: ["hallucination", "retrieval", "in-context-learning"],
  },
  "context-window": {
    term: "Context window",
    short: "The maximum number of tokens a model can attend to at once — both prompt and generated output combined.",
    long: "The context window defines the model's working memory. Tokens outside this window are invisible to the model. Early transformers had context windows of 512-2048 tokens; modern models support 100k+ tokens. Longer context windows enable processing entire documents and long conversations, but increase memory and compute requirements quadratically with naive attention.",
    seeAlso: ["token", "attention", "transformer", "in-context-learning"],
  },
  "in-context-learning": {
    term: "In-context learning",
    short: "The ability of LLMs to adapt their behavior based on examples or instructions provided in the prompt, without weight updates.",
    long: "LLMs can learn new tasks at inference time purely from examples in the context window — no gradient descent required. This emergent capability allows few-shot and zero-shot prompting to work. The mechanism is debated: it may be a form of implicit Bayesian inference or retrieval from pretraining. In-context learning degrades for tasks very dissimilar from pretraining data.",
    seeAlso: ["few-shot", "zero-shot", "prompt", "context-window", "emergent"],
  },
  prompt: {
    term: "Prompt",
    short: "The input text provided to an LLM that conditions the model's generation.",
    long: "A prompt can include instructions, context, examples, conversation history, and a partial output for the model to complete. Prompt engineering is the practice of crafting effective prompts to elicit desired behaviors. System prompts, used in instruction-tuned models, set persistent context and persona. The quality of the prompt strongly influences output quality.",
    seeAlso: ["in-context-learning", "few-shot", "zero-shot", "chain-of-thought"],
  },
  "fine-tuning": {
    term: "Fine-tuning",
    short: "Continuing to train a pretrained model on a smaller, task-specific dataset to adapt it to a particular domain or behavior.",
    long: "Fine-tuning updates the model's weights via gradient descent on labeled examples, specializing the model beyond what pretraining alone provides. Supervised fine-tuning (SFT) on instruction-response pairs is the first step in aligning chat models. Parameter-efficient methods like LoRA fine-tune only small adapter modules rather than all weights, reducing compute cost.",
    seeAlso: ["pretraining", "rlhf", "backpropagation", "weight"],
  },
  rlhf: {
    term: "RLHF",
    short: "Reinforcement Learning from Human Feedback — a training method that uses human preference judgments to align model behavior.",
    long: "RLHF has three stages: (1) supervised fine-tuning on demonstrations, (2) training a reward model on human preference comparisons, and (3) optimizing the LLM policy against the reward model using reinforcement learning (typically PPO). RLHF is key to making models helpful, harmless, and honest. Variants include RLAIF (AI feedback) and DPO (direct preference optimization, which skips the reward model).",
    seeAlso: ["fine-tuning", "alignment", "pretraining"],
  },
  pretraining: {
    term: "Pretraining",
    short: "The large-scale, unsupervised training phase where a model learns language from vast amounts of text via next-token prediction.",
    long: "Pretraining is the computationally expensive first phase of LLM development. The model is trained on trillions of tokens from the web, books, and code using next-token prediction as the self-supervised objective. This builds general-purpose language understanding and world knowledge. Pretraining is followed by fine-tuning and alignment to produce a deployable assistant.",
    seeAlso: ["next-token-prediction", "fine-tuning", "rlhf", "backpropagation", "loss"],
  },
  backpropagation: {
    term: "Backpropagation",
    short: "The algorithm for computing gradients of the loss with respect to all model parameters by applying the chain rule backward through the network.",
    long: "Backpropagation is the workhorse of neural network training. Given a forward pass that computes the loss, backprop applies the chain rule of calculus to compute how much each weight contributed to the error. These gradients are then used by an optimizer (e.g., Adam) to update the weights. Modern deep learning frameworks compute gradients automatically via autograd.",
    seeAlso: ["gradient-descent", "loss", "weight", "parameter"],
  },
  "gradient-descent": {
    term: "Gradient descent",
    short: "An iterative optimization algorithm that updates model parameters in the direction that reduces the loss.",
    long: "Gradient descent updates each parameter by subtracting a small multiple (the learning rate) of the gradient of the loss with respect to that parameter. Stochastic gradient descent (SGD) approximates the gradient using mini-batches of training data. Modern LLMs use Adam, an adaptive variant that maintains per-parameter learning rates. Gradient descent is what makes neural network training possible.",
    seeAlso: ["backpropagation", "loss", "weight", "parameter"],
  },
  loss: {
    term: "Loss",
    short: "A scalar value measuring how wrong the model's predictions are; the quantity that training minimizes.",
    long: "The loss function maps model outputs and ground truth labels to a non-negative number. Lower loss means better predictions. For language models, cross-entropy loss is standard. The training loop computes loss on a batch, backpropagates gradients, and updates weights to reduce it. Loss on a held-out validation set tracks generalization and detects overfitting.",
    seeAlso: ["cross-entropy", "backpropagation", "gradient-descent", "next-token-prediction"],
  },
  "cross-entropy": {
    term: "Cross-entropy loss",
    short: "The standard loss for language models: the negative log-probability assigned to the true next token.",
    long: "Cross-entropy loss H(p, q) = -sum(p(x) * log q(x)). For next-token prediction, p is a one-hot distribution over the true token and q is the model's softmax distribution. The loss reduces to -log(q(true_token)), penalizing the model more when it assigns low probability to the correct answer. Perplexity is the exponentiated average cross-entropy loss.",
    seeAlso: ["loss", "softmax", "logit", "next-token-prediction"],
  },
  "layer-norm": {
    term: "Layer normalization",
    short: "A technique that normalizes activations across the feature dimension within each layer to stabilize training.",
    long: "Layer normalization computes the mean and variance of activations across the hidden dimension for each token independently, then scales and shifts with learned parameters (gamma and beta). Unlike batch normalization, it does not depend on batch statistics, making it well-suited for variable-length sequences and small batches. Transformers apply layer norm before or after each sub-layer (pre-norm vs. post-norm).",
    seeAlso: ["residual", "transformer", "weight"],
  },
  residual: {
    term: "Residual connection",
    short: "A skip connection that adds a layer's input directly to its output, enabling very deep networks to train reliably.",
    long: "Residual (or skip) connections, introduced in ResNet, compute output = F(x) + x rather than output = F(x). This means gradients can flow directly from later layers to earlier layers, mitigating the vanishing gradient problem. In transformers, each sub-layer (self-attention and feed-forward) is wrapped with a residual connection. Without residuals, training deep transformers would be extremely difficult.",
    seeAlso: ["transformer", "layer-norm", "backpropagation"],
  },
  "feed-forward": {
    term: "Feed-forward network",
    short: "A two-layer MLP applied independently to each token position in a transformer block.",
    long: "The feed-forward sublayer consists of two linear projections with a nonlinear activation (typically GELU or ReLU) in between: FFN(x) = max(0, xW_1 + b_1)W_2 + b_2. The intermediate dimension is usually 4x the model's hidden dimension. Unlike attention, the FFN operates on each token independently — it does not mix information across positions. It is thought to store factual associations learned during training.",
    seeAlso: ["transformer", "attention", "residual"],
  },
  head: {
    term: "Attention head",
    short: "One parallel attention computation within a multi-head attention layer, each learning to attend to different patterns.",
    long: "Each attention head has its own learned projection matrices for queries, keys, and values. Running multiple heads in parallel allows the model to jointly attend to information from different representation subspaces — e.g., one head might focus on syntactic relationships, another on coreference. The outputs of all heads are concatenated and projected to produce the final representation.",
    seeAlso: ["multi-head-attention", "attention", "self-attention"],
  },
  "multi-head-attention": {
    term: "Multi-head attention",
    short: "The transformer mechanism that runs several attention computations in parallel then concatenates their outputs.",
    long: "Multi-head attention projects the input into h different (query, key, value) subspaces, computes scaled dot-product attention in each, concatenates the results, and applies a final linear projection. Using h heads with d_k = d_model / h dimensions each keeps total compute comparable to single-head attention while enabling richer representation. This is the core computational primitive of every transformer.",
    seeAlso: ["head", "attention", "self-attention", "transformer"],
  },
  "position-encoding": {
    term: "Positional encoding",
    short: "Information added to token embeddings to tell the model the position of each token in the sequence.",
    long: "Self-attention is permutation-invariant — without positional information, the model cannot distinguish word order. Positional encodings inject position information into embeddings. The original transformer used fixed sinusoidal encodings. Modern LLMs often use learned absolute positions or Rotary Position Embedding (RoPE), which encodes relative positions directly in the attention computation and generalizes better to longer sequences.",
    seeAlso: ["embedding", "attention", "transformer", "token"],
  },
  weight: {
    term: "Weight",
    short: "A learned scalar parameter inside a neural network, adjusted by training to minimize the loss.",
    long: "Weights are the trainable numerical values that define a model's behavior. They are organized into matrices (e.g., attention projection matrices, feed-forward layers, embedding tables). At training time, backpropagation computes gradients and gradient descent updates weights iteratively. A model's 'size' is typically measured in the total number of weights (parameters). At inference time, weights are frozen.",
    seeAlso: ["parameter", "backpropagation", "gradient-descent", "fine-tuning"],
  },
  parameter: {
    term: "Parameter",
    short: "A synonym for weight — any learnable value in a neural network that is updated during training.",
    long: "Parameters include all weight matrices, bias vectors, layer normalization scales and shifts, and embedding tables in a model. The total parameter count is a rough proxy for model capacity. A '7B model' has 7 billion parameters. Not all parameters contribute equally to performance; research on pruning and quantization aims to reduce parameter counts or precision without sacrificing capability.",
    seeAlso: ["weight", "backpropagation", "embedding"],
  },
  vocabulary: {
    term: "Vocabulary",
    short: "The fixed set of all tokens a model can produce or consume, each assigned a unique integer ID.",
    long: "The vocabulary is determined by the tokenizer and fixed at training time. Modern LLMs typically have vocabularies of 32,000 to 128,000 tokens. The embedding table has one vector per vocabulary entry. The final output layer projects to vocabulary size to produce logits. A larger vocabulary can represent text more efficiently but increases memory and the size of the embedding and output layers.",
    seeAlso: ["token", "tokenizer", "embedding", "logit"],
  },
  autoregressive: {
    term: "Autoregressive",
    short: "A generation paradigm where the model produces one token at a time, conditioning each new token on all previously generated tokens.",
    long: "Autoregressive models factorize the joint probability of a sequence as a product of conditional probabilities: P(x_1,...,x_n) = product P(x_i | x_1,...,x_{i-1}). At inference, the model samples or greedily selects one token, appends it to the context, and repeats. This sequential process is why LLM inference is slow relative to training. It also means errors can compound — a wrong early token affects all subsequent ones.",
    seeAlso: ["next-token-prediction", "sampling", "greedy-decoding", "beam-search"],
  },
  "beam-search": {
    term: "Beam search",
    short: "A decoding strategy that tracks the top-k most probable partial sequences (beams) at each step, then returns the best complete sequence.",
    long: "Beam search maintains a beam of b candidate sequences. At each step, each beam is extended with every possible next token, scored by cumulative log-probability, and the top-b candidates are kept. Unlike greedy decoding (b=1), beam search explores multiple hypotheses and often finds higher-probability sequences. It was standard in machine translation but is less common for open-ended generation, where sampling is preferred.",
    seeAlso: ["greedy-decoding", "sampling", "autoregressive"],
  },
  "greedy-decoding": {
    term: "Greedy decoding",
    short: "A deterministic decoding strategy that always selects the single most probable next token at each step.",
    long: "Greedy decoding is simple and fast: at each step, pick argmax(logits). This is equivalent to temperature approaching 0 or beam search with beam size 1. Greedy decoding is deterministic — the same prompt always produces the same output. It tends to produce repetitive text and can get stuck in loops. It is rarely used for open-ended generation but is useful for tasks with single correct answers.",
    seeAlso: ["beam-search", "sampling", "temperature", "logit"],
  },
  agency: {
    term: "Agency",
    short: "The capacity of an AI system to take actions, make decisions, and pursue goals in an environment.",
    long: "An agentic LLM goes beyond text generation to use tools (web search, code execution, APIs), plan multi-step tasks, and operate autonomously over extended periods. Agency increases capability but also introduces new risks: the model can cause real-world effects, errors compound, and it is harder for humans to oversee. Alignment and capability research increasingly focuses on safe agentic systems.",
    seeAlso: ["alignment", "capability", "emergent"],
  },
  alignment: {
    term: "Alignment",
    short: "The research and engineering effort to ensure AI model behavior matches human values, intentions, and safety requirements.",
    long: "An aligned model is helpful, honest, and avoids causing harm. Alignment techniques include RLHF, constitutional AI, debate, and interpretability research. Alignment is hard because human values are complex and sometimes contradictory, models can behave differently in deployment than in training, and capable models may find unexpected ways to satisfy proxy objectives. It is considered a central challenge in AI safety.",
    seeAlso: ["rlhf", "capability", "agency", "hallucination"],
  },
  capability: {
    term: "Capability",
    short: "What a model can do — its skills and performance across tasks like reasoning, coding, math, and language.",
    long: "Capability refers to the range and quality of tasks a model can perform. Capabilities emerge from scale, data quality, and training techniques. Key capabilities include instruction following, coding, multi-step reasoning, and tool use. Capability evaluations (benchmarks) measure performance systematically. Rapid capability growth has spurred increasing focus on alignment and safety to ensure powerful capabilities are beneficial.",
    seeAlso: ["emergent", "alignment", "agency", "llm"],
  },
  emergent: {
    term: "Emergent behavior",
    short: "Abilities that appear in large models but were not explicitly trained for and are absent in smaller models.",
    long: "Emergent capabilities appear suddenly as model scale crosses certain thresholds, rather than improving smoothly. Examples include few-shot arithmetic, chain-of-thought reasoning, and instruction following. The phenomenon is debated: some argue emergence is real and surprising; others contend it is an artifact of discontinuous evaluation metrics. Understanding emergence is important for predicting and governing future model capabilities.",
    seeAlso: ["capability", "llm", "in-context-learning", "few-shot"],
  },
  "few-shot": {
    term: "Few-shot prompting",
    short: "Providing a small number of input-output examples in the prompt to demonstrate the desired task format or behavior.",
    long: "Few-shot prompting leverages in-context learning: the model infers the task from the examples without weight updates. Typically 1-10 examples are included in the context. More examples generally help but consume context window space. Few-shot prompting works best when examples are high-quality, representative, and formatted consistently. It was a key technique before instruction-tuned models became widely available.",
    seeAlso: ["zero-shot", "in-context-learning", "prompt", "context-window"],
  },
  "zero-shot": {
    term: "Zero-shot prompting",
    short: "Instructing a model to perform a task with no examples, relying entirely on knowledge from pretraining and instruction tuning.",
    long: "Zero-shot prompting provides only a task description or instruction, with no demonstrations. Modern instruction-tuned LLMs perform surprisingly well zero-shot because fine-tuning has taught them to follow natural language instructions. Zero-shot performance is a measure of a model's general capability and instruction following. When zero-shot fails, adding a few examples (few-shot) or chain-of-thought often helps.",
    seeAlso: ["few-shot", "in-context-learning", "prompt", "fine-tuning"],
  },
  "chain-of-thought": {
    term: "Chain-of-thought",
    short: "A prompting technique that encourages models to produce explicit intermediate reasoning steps before giving a final answer.",
    long: "Chain-of-thought (CoT) prompting, introduced by Wei et al. 2022, dramatically improves performance on multi-step reasoning tasks (math, logic, commonsense). By adding 'Let's think step by step' or including worked-out reasoning examples, the model externalizes its reasoning. CoT works because it breaks hard problems into easier sub-steps and may allow the model to use token computation more efficiently. It is most effective in large models.",
    seeAlso: ["few-shot", "zero-shot", "prompt", "emergent"],
  },
  retrieval: {
    term: "Retrieval-augmented generation",
    short: "A technique that fetches relevant external documents at inference time and includes them in the prompt to ground model responses.",
    long: "RAG (Lewis et al., 2020) combines a retriever (typically a vector similarity search over embedded documents) with a generator (the LLM). Given a query, the retriever finds the most relevant passages from a knowledge base, which are prepended to the prompt. The LLM then generates an answer conditioned on both the query and retrieved context. RAG reduces hallucination, enables up-to-date knowledge, and improves attribution.",
    seeAlso: ["grounding", "hallucination", "embedding", "in-context-learning", "context-window"],
  },
};

export default glossary;
